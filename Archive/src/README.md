# src module map

This folder is reserved for the next modular refactor.

The runtime has been restored to the previous single-SVG slideshow and previous View Options drawer. New modules should only be added here when they replace old runtime work without adding extra frame-loop overhead.
